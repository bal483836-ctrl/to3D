# 复制为 .env 并按需修改：cp .env.example .env

# 适配器：mock(默认,无 GPU 可跑) 或 http(接真实混元 3D 推理服务)
TO3D_ADAPTER=mock

# 当 TO3D_ADAPTER=http 时必填：混元 3D 推理服务地址
# 例如 http://hunyuan3d.internal:9000
TO3D_HUNYUAN_ENDPOINT=

# 产物(glb/obj)输出目录
TO3D_OUTPUT_DIR=/tmp/to3d_outputs

# 推理请求超时(秒)
TO3D_REQUEST_TIMEOUT=600
